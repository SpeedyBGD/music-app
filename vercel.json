{
  "buildCommand": "cd client && npm ci --include=optional || npm install --include=optional && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "cd client && npm install --include=optional",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

